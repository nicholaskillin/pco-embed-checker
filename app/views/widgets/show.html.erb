<div class="row">
  <%= render 'shared/widget_side_nav' %>
  <div class="col-sm-8">
    <div class="row">
      <h1><%= @widget.name %></h1> - <%= link_to 'Delete', 
                                                widget_path(@widget), 
                                                method: :delete, 
                                                data: { confirm: 'Are you sure?' } %> 
    </div>
    <div class="row">
      <%= @widget.code.html_safe %>
    </div>
  </div>
</div>