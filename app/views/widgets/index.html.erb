<div class="row">
  <div class="col-sm-3" id="side-nav">
    <ul class="nav flex-column">
      <div class="widget-index">
        <% if @widget_index.any? %>
          <ul class="widgets">
            <% @widget_index.each do |w| %>
              <li><%= w.name %></li>
            <% end %>
          </ul>
        <% end %>
      </div>
    </ul>
    <div class="widget-form">
      <%= render 'widget_form'%>
    </div>
  </div>
  <div class="col-sm-8">
    Widget Index
  </div>
</div>